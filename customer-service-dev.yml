spring:
  application:
    name: student-service
  datasource:
    username: postgres
    password: 1234
    url: jdbc:postgresql://localhost:5432/student_service_db
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081